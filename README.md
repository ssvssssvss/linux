После удаления файла /opt/script.sh процесс продолжил выполнение. Для обнаружения исходного кода понадобилось три команды:
```bash
ps aux | grep /opt/script.sh 		# определение PID процесса
sudo ls -l /proc/*/fd | grep script.sh	# проверка состояния файла
cat /proc/1707/fd/255			# вывод исходного кода
